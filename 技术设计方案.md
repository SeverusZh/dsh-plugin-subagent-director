# 技术设计方案 v0.1 —— Subagent Director（子代理导演）
## dsh-plugin-subagent-director

| 项目 | 内容 |
|---|---|
| 版本 | v0.1（2026-08-14） |
| 作者 | 领导者/主代理（规划与审查职责） |
| 上游依据 | 《需求文档.md》v0.2 + DSH 0.1.0-rc.3 源码事实（E:\DeepSeekHarness\node_modules\@deepseek-ai\） |
| 状态 | 已定稿，作为 M1 实施依据 |

---

## 0. 命名决议（最终）

| 项 | 值 |
|---|---|
| 项目名（EN/中文） | Subagent Director「子代理导演」 |
| npm 包名 | dsh-plugin-subagent-director |
| Cordis 插件名 | subagent-director |
| settings 命名空间 | subagent-director |
| 模型可见工具名（默认） | subagent_role |
| GitHub 仓库 | dsh-plugin-subagent-director（发布阶段创建） |
| 目录名（本地） | dsh-subagents-options（保留，历史代号） |

## 1. 范围与文档关系

- 《需求文档.md》= WHAT（功能需求 FR-1~12、NFR、验收标准）；本设计 = HOW。
- 命名冲突时以本设计 §0 为准；需求文档已按命名决议修订。
- M1 范围 = 需求文档 §10 的 P0 主机侧闭环（FR-1/2 的运行时部分、FR-3、FR-4 数据模型、FR-5.1~5.4 前景路径、FR-8、FR-10、FR-11、FR-12.1）。

## 2. 实测结论：当前委派机制的模型路由现状（2026-08-14）

> 通过解码子代理会话日志（tools/decompress-session.cjs）获得地面真相。

| 委派通道 | provider/model 覆盖是否生效 | 证据 |
|---|---|---|
| subagent 工具（额外传 provider/model 参数） | **不生效**（参数被静默忽略，子代理继承父模型 opencode-go/deepseek-v4-pro） | continuable 子代理 descriptor：agentProvider=opencode-go, agentModel=deepseek-v4-pro；request/header.config 同 |
| workflow 工具 agent(prompt, {provider, model}) | **生效** | one-shot 子代理 request/context 与 replayState 均为 provider=opencode-go, model=deepseek-v4-flash（含真实 responseId） |
| dsh-tool-subagent 静态 config.agentOptions | 生效（静态，非动态） | 官方 README |

**设计含义**：
1. 本项目要解决的"subagent 工具动态选模型"缺口被实测证实（需求文档 F1 的运行时证据）。
2. 本项目开发过程中，**领导者派发执行者/观察者子代理一律使用 workflow 工具的 agent() opts 通道**（provider=opencode-go；执行者 model=deepseek-v4-flash，多模态观察者 model=Minimax-M2.7）。
3. 我们的插件实现（自定义工具注入 agentOptions）与 workflow 的实现路径同源，风险低。

## 3. 总体架构

```text
┌────────────── 用户 / 模型 / 会话 ──────────────┐
│ 设置 UI（M2）          委派工具 subagent_role     │
│ settings.section        args: role/provider/model │
└──────────────────┬──────────────────┬───────────┘
                   ▼                  ▼
┌────────────────── Host 插件（本包）──────────────────┐
│ settings.ts  installSettingsSection('subagent-director') │
│ route-resolver.ts  resolveRoute() 纯函数（四层链）     │
│ delegation-tool.ts defineTool('subagent_role')        │
│ guidance.ts    ctx.systemPrompt.section() 角色清单     │
│ index.ts       装配：可选依赖降级、provider 事件等待    │
└──────────────────┬───────────────────────────────────┘
                   ▼ ctx.subagents.start(name, { agentOptions, persona, ... })
┌────────────────── DSH seam（官方，零修改）────────────┐
│ resolveChildAgentOptions(parent, requested, depth)     │
│   → 继承父模型，requested 字段级覆盖                    │
│ applyChildComposition → persona shadow                  │
│ descriptor → 记录 agentProvider/agentModel（continuable）│
└────────────────────────────────────────────────────────┘
```

## 4. 包结构与开发工作流

### 4.1 目录树（M1 目标态）

```text
dsh-subagents-options/
├── package.json
├── tsconfig.json
├── vitest.config.ts
├── .gitignore
├── README.md
├── 需求文档.md
├── 技术设计方案.md
├── tools/
│   └── decompress-session.cjs        # 会话日志解码开发工具
├── src/
│   ├── index.ts                      # host 插件入口
│   ├── config.ts                     # 插件 Config（cordis 层）
│   ├── settings.ts                   # settings ns + schema + validate
│   ├── route-resolver.ts             # 四层路由解析（纯函数）
│   ├── delegation-tool.ts            # subagent_role 工具
│   ├── guidance.ts                   # 主 agent 角色指引段落
│   └── client/
│       └── index.ts                  # client 入口（M2 实现，M1 仅占位）
├── test/
│   ├── route-resolver.test.ts
│   ├── settings-schema.test.ts
│   └── tool-schema.test.ts
└── lib/                              # tsc 产物（gitignore）
```

### 4.2 依赖策略

- **peerDependencies**：@deepseek-ai/cordis、dsh-agent、dsh-llm、dsh-settings、dsh-subagent、dsh-system-prompt、dsh-tools、dsh-tool-jobs（按实际用到增减）。发布后由 profile 的 pnpm 解析（@deepseek-ai/* 为 restricted，不可由第三方直接 npm 安装——**本包绝不 bundle DSH 包**）。
- **devDependencies**：上述 peer 以 file: 链接到 E:\DeepSeekHarness\node_modules（相对路径 ..\..\DeepSeekHarness\node_modules\@deepseek-ai\<pkg>，用 npm install <path> 生成）；typescript、vitest 走 npm registry（实测 npm ping 可达）。
- **脚本**：build = tsc -p tsconfig.json；test = vitest run；typecheck = tsc --noEmit。
- 运行环境：Node v24.19.0、npm 11.17.0（本机无 pnpm；DSH checkout 自带 node_modules 仅供只读参考与 file: 链接）。

### 4.3 本地验证方式（人工冒烟）

1. npm run build 产出 lib/；
2. 在 profile 的 cordis.patch.yml 或 package.json 中以 file: 路径挂载本包（web profile 示例配置写入 README）；
3. 重启 profile，设置页出现配置（M2 后），对话中调用 subagent_role 委派，检查子代理会话日志 request/context 的 provider/model 与配置一致（复用 tools/decompress-session.cjs 验证）。
## 5. 配置设计

### 5.1 插件 Config（cordis 层，src/config.ts）

```ts
export interface DirectorConfig {
  /** subagent 传输 provider：spawn | fork | 其他已注册 provider。默认 'spawn' */
  subagentProvider?: string
  /** 模型可见工具名，默认 'subagent_role'；必须与已加载工具名不同 */
  toolName?: string
  /** 是否暴露 run_in_background，默认 true */
  enableRunInBackground?: boolean
  /** 后台生命周期策略，默认 'one-shot'（'continuable' 为 M2） */
  backgroundMode?: 'one-shot' | 'continuable'
  /** 委派深度上限，默认 3；'provider-managed' 表示不下发 */
  maxDepth?: number | 'provider-managed'
}
```

说明：字段语义对齐 dsh-tool-subagent/lib/types/index.d.ts（F2），保持生态一致性。

### 5.2 settings 命名空间 subagent-director（src/settings.ts）

```ts
export interface RoleTemplate {
  displayName: string          // 必填，非空
  description: string          // 必填，给主 agent 的委派指引
  persona?: string             // 注入子代理的 persona
  provider?: string            // LLM route 覆盖（可选）
  model?: string               // 模型覆盖（可选）
  reasoningEffort?: string     // 可选
  toolFilter?: { allow?: string[]; deny?: string[] }  // 可选（M3 启用运行时注入）
}

export interface SubagentDirectorSettings {
  defaultProvider?: string
  defaultModel?: string
  defaultReasoningEffort?: string
  defaultRole?: string           // 默认角色模板 id
  fallbackOnInvalid?: boolean    // 默认 true：角色模型失效时回退父模型
  roles?: Record<string, RoleTemplate>
}
```

- 注册方式：installSettingsSection(ctx, NS, Schema, entry, { setSource, onChange, validate })（参照 dsh-llm-pi-ai/lib/index.js:1846-1865）。
- validate 职责（写时拒绝）：displayName/description 非空；roles 键合法（kebab-case）；defaultRole 引用存在；显式 provider 非空字符串。
- **运行期校验与写时校验分层**：provider 是否可路由、model 是否在目录，属运行期事实，写时只做格式校验（对齐 agentDefaultModel "不校验目录成员" 原则），运行期按 §7 错误矩阵处理。

## 6. 路由解析器（src/route-resolver.ts，纯函数核心）

```ts
export type RouteLayer = 'call' | 'role' | 'default' | 'inherit'

export interface RouteInput {
  args?: { role?: string; provider?: string; model?: string; reasoningEffort?: string }
  settings: SubagentDirectorSettings
  parent: { provider?: string; model?: string }   // 父 agent options（inherit 层）
}

export interface RouteResult {
  layer: RouteLayer
  agentOptions?: { provider?: string; model?: string; reasoningEffort?: string }  // 仅含用户显式配置的字段
  persona?: string
  roleId?: string
  toolFilter?: { allow?: string[]; deny?: string[] }
  warnings: string[]               // 无效角色引用、失效模型等（由调用方决定报错/回退）
}

export function resolveRoute(input: RouteInput): RouteResult
```

解析顺序（字段级独立解析）：
1. **call**：args.provider / args.model / args.reasoningEffort（显式参数，逐字段独立生效）；
2. **role**：args.role（或 defaultRole）对应的 roles[id] 绑定值，填补 call 未覆盖的字段；persona/toolFilter 只来自角色层；
3. **default**：settings.defaultProvider/defaultModel/defaultReasoningEffort 填补；
4. **inherit**：以上皆无的字段不注入（交由 seam 继承父 agent）。

测试矩阵（≥12 用例，test/route-resolver.test.ts）：
- 四层各有值 / 各缺字段的逐层覆盖；
- call 层显式空字符串视为未指定（跳过）；
- role 不存在 → warnings + 降层；
- defaultRole 指向不存在角色 → warnings + 降层；
- persona/toolFilter 仅角色层产出；
- inherit 层：结果 agentOptions 为 undefined 或空对象（零侵入，AC-3.2）。

## 7. 委派工具 subagent_role（src/delegation-tool.ts）

### 7.1 模型可见 schema

```jsonc
{
  "description": { "type": "string", "required": true },
  "prompt":       { "type": "string", "required": true },
  "role":         { "type": "string", "description": "角色模板名（可选）。未填用默认角色。" },
  "provider":     { "type": "string", "description": "LLM 供应商覆盖（可选）。显式指定优先于角色绑定。" },
  "model":        { "type": "string", "description": "模型名覆盖（可选）。显式指定优先于角色绑定。" },
  "reasoningEffort": { "type": "string", "description": "推理强度覆盖（可选）。" },
  "run_in_background": { "type": "boolean", "description": "是否后台运行（enableRunInBackground 开启时可见）。" }
}
```

### 7.2 execute 流程

1. 读取 args；调用 resolveRoute；
2. **显式 provider/model 运行期校验**（FR-8.1）：args 显式指定但 ctx.llm 未注册该 route / adapter 不服务 → 返回结构化错误（含可用 route 列表），绝不静默回退；
3. 角色绑定模型失效：fallbackOnInvalid=true → 丢弃该字段 + warnings 透传（结果附告警）；=false → 报错；
4. persona 注入前 capability 预检：ctx.subagents.getProvider(name).capabilities.persona 不存在 → 报错并建议；
5. 组装 SubagentStartRequest：label=description、prompt、parent、agentOptions（resolveRoute 结果）、persona、maxDepth；
6. ctx.subagents.start(config.subagentProvider, request)：
   - 前景（M1 核心）：await run.result → 完成输出；异常 → 带 stop-reason 的错误结果（对齐 dsh-tool-subagent 的前景收集语义）；
   - one-shot 后台（M1 附带，镜像 dsh-tool-subagent 的 Task 注册路径，返回 { kind:'background', jobId }）；
   - continuable（M2）。
7. 所有路径 dispose 幂等（对齐官方 README "One-shot ownership and lifecycle"）。

### 7.3 错误契约（FR-8）

错误文本统一格式：subagent-director: <简明原因>。可用供应商: a, b, c；不含凭据；无效值 + 可用列表同条给出。

## 8. 主 agent 指引段落（src/guidance.ts）

- 用 ctx.systemPrompt.section() 注册段落（可选依赖，缺 systemPrompt 服务则跳过，零侵入）；order 取 117（位于 dsh-tool-subagent 的 tool 段落 116.5 之后，保持就近关联）。
- 内容：Subagent Director 角色清单：每个角色的 displayName、description、绑定模型（若有）、委派语法示例（role 参数）；无角色时不注册（FR-6.3）。
- 变更只影响新组装（KV cache 稳定，NFR-3）。

## 9. 客户端 UI（M2 设计预览）

- client/index.ts：ctx.slots.inject() 注册 settings.section { id: 'subagent-director', order, label } + LocaleNamespaceMap（zh/en）。
- store：SnapshotStore 组合 llm.providers + llm.models + settings.describe；写入 settings.mutate + expectedRevision（参照 dsh-client-ui-settings-models/lib/types/client/store.d.ts）。
- 组件：SubagentOptionsSection（默认模型行 + 角色卡片列表）、RoleCard（名称/描述/persona/供应商下拉/模型联动下拉/推理强度/删除/设为默认）；表单手写 + dsh-client-schema-form helpers。
- 不复用 ModelDirectory（subagent 会话 agent-busy 限制，F7）。

## 10. 可观测性与日志（FR-7 运行时部分）

- 每次委派日志：请求参数、resolveRoute 命中层（layer）、最终 agentOptions/persona、warnings。
- 子代理实际模型以 descriptor（continuable）/ 会话 request/context（通用）为权威，用 tools/decompress-session.cjs 可人工核验；M3 再做 UI 展示。

## 11. 测试计划

| 层 | 内容 | 工具 |
|---|---|---|
| 单元 | route-resolver 矩阵 ≥12 例；settings schema 校验（非法角色键、空 displayName、defaultRole 悬空）；工具 schema 形状断言 | vitest |
| 冒烟（人工） | §4.3 流程：挂载 → 对话委派 → 子代理日志 request/context 断言 provider/model | tools/decompress-session.cjs |
| 安全审查 | FR-11 清单逐项核对（凭据不落 settings、provider 白名单、错误信息 redact） | 领导者审查 |

## 12. 里程碑与派发计划（领导者派发，执行者实施）

| 里程碑 | 内容 | 交付物 | 派发方式 |
|---|---|---|---|
| M1（本轮启动） | Host 核心：脚手架、config、settings、route-resolver、subagent_role 工具（前景 + one-shot 后台）、guidance、单测 | 源码 + 测试全绿 + README 初稿 + 人工冒烟步骤 | workflow agent({provider: opencode-go, model: deepseek-v4-flash}) |
| M2 | 设置 UI（client 入口 + 两个组件 + store + i18n） | UI 可运行 | 同上（执行者）；截图核对时用观察者 Minimax-M2.7 |
| M3 | 体验完善：continuable 后台、toolFilter 注入、结果可观测性、FR-6 打磨 | 增量 | 同上 |
| M4 | 增强：replaceStockSubagent、provider 装饰器兜底（方案 B） | 增量 | 同上 |

执行者工作纪律（写进每次派发 prompt）：
- 只写项目目录；E:\DeepSeekHarness 只读参考；
- 引用 DSH API 前先读该包 lib/types/*.d.ts 与 lib/index.js 实证，禁止臆测；
- 每项功能配单测；结束输出结构化报告（文件清单、测试结果、遗留问题）。

## 13. Git 与开源发布计划

- 仓库初始化：git init（main 分支）；.gitignore：node_modules/、lib/、*.log、.DS_Store。
- Commit 规范：docs:/feat:/test:/fix:/chore: 前缀 + 中文说明；领导者审查后提交（领导者执行 git 操作）。
- 里程碑提交：v0.1 需求+设计；v0.2 M1 核心；v0.3 M2 UI；v1.0 开源发布。
- GitHub：dsh-plugin-subagent-director；LICENSE=MIT（与 DSH 一致）；README.zh.md/README.md（安装 dsh plugin add、配置示例、角色模板示例、原理说明）；CHANGELOG。
- npm 发布注意：@deepseek-ai/* 为 restricted → 本包只声明 peerDependencies，由使用方 profile 提供；发布前核对 dsh-plugin-product-subagents 功能重叠（OQ-6）。

## 14. 风险清单

| # | 风险 | 缓解 |
|---|---|---|
| R1 | DSH rc 版本 API 漂移 | 全部接口引用锁定 0.1.0-rc.3 + 文件路径；升级时回归 |
| R2 | provider 双命名空间混淆 | 代码注释 + README 显式区分 subagentProvider(传输) 与 provider(LLM route) |
| R3 | 工具名冲突 | 加载前校验 toolName 与已注册工具名不重复，冲突报错 |
| R4 | KV cache 失效 | 配置变更只影响新组装；schema/段落前缀稳定 |
| R5 | 后台路径复杂度（Task 注册） | M1 若镜像成本过高则降级为前景 + 显式拒绝后台参数（记录在 M1 报告） |
| R6 | 无法在本机真实挂载冒烟 | 冒烟步骤文档化，作为人工验收清单交付 |
| R7 | 执行者子代理能力边界 | 派发前验证工具可用性（write/edit/pwsh/npm）；失败时领导者补位决策 |

---

## 附录 A：派发机制实测记录（2026-08-14）

1. subagent 工具 + provider/model 参数 → 子代理 sessionId 50c76d5a…：descriptor 记录 agentProvider=opencode-go、agentModel=deepseek-v4-pro（=父模型），参数未生效。
2. workflow 工具 agent(prompt, {provider:'opencode-go', model:'deepseek-v4-flash'}) → 子代理 sessionId 27c92ffc…：request/context 与 replayState 均记录 opencode-go/deepseek-v4-flash，覆盖生效。
3. 结论：开发期一律经 workflow 通道派发执行者/观察者；本插件交付后，用户无需依赖 workflow 也能对 subagent 工具动态选模型（本项目的核心价值）。

## 附录 B：会话日志解码工具

tools/decompress-session.cjs：按 zstd 帧 magic(28B52FFD) 扫描并逐帧解压 DSH 会话日志（node:zlib 内置 zstd），过滤 provider/model/descriptor 相关行。用法：

```text
node tools/decompress-session.cjs <session.jsonl.zstd 路径>
```

---

*（设计定稿。M1 实施派发见 §12；领导者将按 §13 执行 Git 提交与审查。）*
