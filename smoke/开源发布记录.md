# 开源发布记录（v0.1.0）

> 执行者：Subagent Director 发布执行器 · 2026-08-14
> 仓库：`SeverusZh/dsh-plugin-subagent-director`（public）
> 版本：`v0.1.0`

## 前置状态核验

- git 工作区干净 ✔
- main 分支 8 个提交（无 remote、无 tag）✔
- gh CLI 2.97 已登录 `SeverusZh`（repo 权限）✔
- LICENSE / CHANGELOG / README / package.json 发布元数据已备好 ✔
- npm 未登录（本轮不碰 npm，只记录）✔

## Step 1 · 发布前回归（全绿 ✔）

- `npm test`：**99 用例全绿**（7 个测试文件：route-resolver 15 / settings-schema 15 / client-store 23 / subagent-model 16 / tool-schema 10 / toolfilter-capability 10 / delegation-mode 10），vitest 4.1.10，0 failed。
- `npm run typecheck`：`tsc --noEmit` 退出码 0，无类型错误。
- `npm run build`：`tsc -p tsconfig.json && node scripts/build-client.mjs` 退出码 0；build-client 写出 `lib/client/index.js`（1475 lines, 48863 bytes）。


## Step 2 · 建仓（public）✔

```
gh repo create SeverusZh/dsh-plugin-subagent-director --public --source . --remote origin --description "Subagent Director: per-subagent LLM provider/model selection with role templates for DeepSeek Harness (dsh plugin)"
> https://github.com/SeverusZh/dsh-plugin-subagent-director
```

- 仓库已创建（public），origin remote 已设为 SSH：`git@github.com:SeverusZh/dsh-plugin-subagent-director.git`

## Step 3 · 推送 main

- `git push -u origin main` 成功：`* [new branch] main -> main`，`main` 已跟踪 origin/main。

## Step 4 · 打标 v0.1.0

- `git tag v0.1.0` 创建成功（本地 tags 含 v0.1.0）。
- `git push origin v0.1.0` 成功：`* [new tag] v0.1.0 -> v0.1.0`。

## Step 5 · GitHub Release v0.1.0


## Step 6 · 核验证据 ✔

```json
// gh repo view SeverusZh/dsh-plugin-subagent-director --json name,visibility,url,defaultBranchRef,createdAt
{"createdAt":"2026-08-14T12:05:44Z","defaultBranchRef":{"name":"main"},"name":"dsh-plugin-subagent-director","url":"https://github.com/SeverusZh/dsh-plugin-subagent-director","visibility":"PUBLIC"}
```

```
// git remote -v
origin	git@github.com:SeverusZh/dsh-plugin-subagent-director.git (fetch)
origin	git@github.com:SeverusZh/dsh-plugin-subagent-director.git (push)

// git tag
v0.1.0
```

```json
// gh release view v0.1.0 --json name,tagName,url,publishedAt
{"name":"v0.1.0 — Subagent Director 首个版本","publishedAt":"2026-08-14T12:06:25Z","tagName":"v0.1.0","url":"https://github.com/SeverusZh/dsh-plugin-subagent-director/releases/tag/v0.1.0"}
```

```
// git ls-remote --tags origin
d304c67ac6be6ca5280fbda3da6e54fa36e85620	refs/tags/v0.1.0
```

## Step 7 · 收尾 ✔

- `git status`：工作区干净（main 与 origin/main 同步）。
- **剩余步骤**：npm 发布需用户先 `npm login`，然后 `npm publish --access public`；发布后 `npm view dsh-plugin-subagent-director` 复核。

---
*记录完成 · 2026-08-14 · 所有步骤全绿，主线红线上未触发任何操作。*
