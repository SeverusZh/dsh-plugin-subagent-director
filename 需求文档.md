# DSH 插件需求文档：Subagent 模型选择与角色规划
## （项目代号：dsh-subagents-options；正式项目名：Subagent Director「子代理导演」，发布名：dsh-plugin-subagent-director）

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.1（初稿，供评审） |
| 状态 | 需求评审中（Draft） |
| 作者 | 主代理（研究 + 编写） |
| 目标平台 | DeepSeek Harness（DSH）0.1.0-rc.3（`@deepseek-ai/dsh`） |
| 调研基准 | 本机安装版 E:\DeepSeekHarness\node_modules\@deepseek-ai\（0.1.0-rc.3 编译产物）+ 联网调研 |
| 关联需求 | "在 DeepSeekHarness 中指定 subagent 使用的模型（供应商 + 模型名），并可规划主 agent 与 subagent 的职责、角色" |

> **命名决议（v0.2，随《技术设计方案》生效）**：正式项目名 **Subagent Director**（中文「子代理导演」）；npm 包名 `dsh-plugin-subagent-director`；Cordis 插件名 `subagent-director`；settings 命名空间 `subagent-director`；模型可见工具默认名 `subagent_role`。本文初稿中的 `dsh-plugin-subagent-options` / `subagent-options` 相应作废，以本决议为准。

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [现状调研：DSH 代码事实](#2-现状调研dsh-代码事实)
3. [行业调研](#3-行业调研)
4. [术语表](#4-术语表)
5. [用户画像与用户故事](#5-用户画像与用户故事)
6. [功能需求（FR）](#6-功能需求fr)
7. [非功能需求（NFR）](#7-非功能需求nfr)
8. [架构与方案设计](#8-架构与方案设计)
9. [UI/UX 流程设计](#9-uiux-流程设计)
10. [需求优先级与里程碑](#10-需求优先级与里程碑)
11. [验收标准汇总](#11-验收标准汇总)
12. [风险与开放问题](#12-风险与开放问题)
13. [参考资料](#13-参考资料)

---

## 1. 背景与目标

### 1.1 背景

DeepSeek Harness（DSH）是一个"一切皆插件"（Everything is a Plugin）的 agent 执行框架，基于 Cordis 插件体系。主 agent 通过 subagent 机制把任务委派给子代理：既有 `subagent`/`subagent_fork` 工具（一次性/上下文分叉委派）、continuable（可续聊）子代理，也有 `workflow`、`ralph` 等编排工具。

用户的核心诉求有三层：

1. **能指定 subagent 用哪个供应商（provider）**——例如主 agent 用 DeepSeek，子代理用第三方网关（pi-ai 等）上的其他模型；
2. **能指定 subagent 用哪个模型名（model）**——例如"主模型干重活、便宜模型干杂活"的成本/能力分层；
3. **最好能规划主 agent 与 subagent 的职责、角色**——例如定义"代码审查员""翻译员""调研员"等角色，并为每个角色绑定专属模型与行为指引。

### 1.2 目标（一句话）

> 提供一个 DSH 插件，让用户在设置界面（及对话中）为 subagent 选择 LLM 供应商与模型，并以"角色模板"的方式规划主 agent 与 subagent 的分工、职责与行为，最终将选择可靠地应用到每次 subagent 委派（含 fork/spawn、后台与 continuable 路径）。

### 1.3 范围

**In scope（本期）**

- 供应商（LLM provider route）的选择、枚举与可用性校验；
- 模型名的选择（按供应商过滤、目录加载、失效处理）；
- 配置层级与回退规则：对话内显式指定 > 角色模板绑定 > 插件默认 > 继承父 agent（现状）；
- "角色模板"：为 subagent 定义角色（名称、职责描述、persona 文案），并可选绑定模型；
- 主 agent 委派指引：向主 agent 说明存在哪些角色、何时委派给谁；
- 设置界面（Settings UI）与持久化；
- 与 DSH 既有 subagent 机制的兼容（后台、continuable、深度限制、沙箱/审批策略继承）。

**Out of scope（本期不做，留待后续）**

- 修改 DSH 上游内核代码（本插件纯树外插件实现，不改 `@deepseek-ai/*` 官方包）；
- 自动成本预算/计费控制（仅预留 allowlist 与默认值护栏）；
- 自动按任务复杂度做模型路由（AI 路由器），本期只做"用户显式配置 + 模型可选参数"；
- 编排主 agent 的多智能体工作流引擎（workflow/ralph 已有，本插件不重复造轮子，但预留与其的默认模型兜底协作）。

### 1.4 成功指标

1. 用户无需编辑 YAML 即可在设置界面完成"供应商 + 模型"配置（≤ 5 次点击/角色）；
2. 通过 `subagent` 类工具发起的一次委派，子代理实际使用用户指定供应商/模型的正确率 100%（有明确指定时）；
3. 角色模板的 persona 与职责描述确实注入子代理会话（可通过子代理输出验证）；
4. 未配置/配置无效时，行为退化到现状（继承父 agent 模型），不产生新的失败模式；
5. 插件不破坏 KV cache 稳定性（见 [NFR](#7-非功能需求nfr)）。

---

## 2. 现状调研：DSH 代码事实

> 结论全部来自本机安装的 `@deepseek-ai/*` 0.1.0-rc.3 编译产物（E:\DeepSeekHarness\node_modules\@deepseek-ai\），关键结论附文件路径，可复查。

### 2.1 关键事实清单

| # | 事实 | 证据 |
|---|---|---|
| F1 | `subagent`/`subagent_fork` 工具的 **schema 只有 `description`、`prompt`、`run_in_background`**，模型在调用时**无法**指定 model/provider | `dsh-tool-subagent/lib/index.js`（parameters schema）、`lib/types/index.d.ts` |
| F2 | 该工具支持**每插件实例静态配置** `agentOptions: { provider, model, maxTokens }`，对所有子代理生效；README 明言 "Child policy is fixed per instance — another model, persona, tool filter, or depth cap requires another distinctly named tool" | `dsh-tool-subagent/README.md`、`lib/types/index.d.ts` |
| F3 | **底层 seam 已完备支持 per-start 覆盖**：`SubagentStartRequest` 含 `agentOptions`、`persona`、`toolFilter`、`maxDepth`、`outputSchema`；`resolveChildAgentOptions(parent, requested, childDepth)` 实现"继承父 agent 的 provider/model/maxTokens，requested 字段级覆盖" | `dsh-subagent/lib/types/types.d.ts`、`lib/types/child-agent.d.ts`、`lib/index.js:505` |
| F4 | **`workflow` 工具是已验证的参考实现**：其 `meta.phases[].provider/model` 与 `agent(prompt, {provider, model})` 支持 per-subagent 模型覆盖，最终落到 `ctx.subagents.start(provider, { agentOptions: {provider, model} })` | `dsh-tool-workflow/lib/index.js:184-194`、`dsh-workflow-worker-thread/lib/index.js:479-492` |
| F5 | `ralph` 工具**不支持**模型覆盖：固定 `config.subagentProvider`（默认 `spawn`） | `dsh-tool-ralph/lib/index.js`、`lib/types/index.d.ts` |
| F6 | 全局默认模型由 `ctx.agentDefaultModel` 服务提供（`{provider, model, reasoningEffort?}`），settings 命名空间 `agent-default-model`；仅 entry point（headless/apiproxy）消费；subagent 默认**继承父 agent** 的 options | `dsh-agent-default-model/README.md`、`lib/index.js` |
| F7 | 现有会话级模型选择 UI（`dsh-client-ui-model-selection`）**明确排除 subagent 会话**（对 subagent 报 `agent-busy`），不可直接复用其目录组件，但可复用其数据通路与类型 | `dsh-client-ui-model-selection/README.md`、`dsh-host-apiproxy/lib/types/api/sessions.d.ts` |
| F8 | LLM 抽象：`ctx.llm` 提供 `registerConfigurableProviders`、`listProviders()`、`listModels(provider)`；`deepseek-official` 为单 provider；`dsh-llm-pi-ai` 支持**多 provider route + 用户 profile**（route 即供应商） | `dsh-llm/lib/index.js`、`dsh-llm-deepseek/lib/index.js`、`dsh-llm-pi-ai/lib/index.js:1726-1865` |
| F9 | **两个命名空间必须区分**：subagent provider（传输实现：`spawn`/`fork`/`acp`…）与 LLM provider route（`deepseek-official`、pi-ai route）是两套名字 | `dsh-tool-subagent`（provider=传输）vs `AgentOptions.provider`（LLM route） |
| F10 | per-child 角色机制存在：`SubagentStartRequest.persona`（需 provider 的 `persona` capability，服务在启动前校验并拒绝不支持者）+ `applyChildComposition` 中 shadow `deployment:persona` | `dsh-subagent/lib/types/types.d.ts`、`lib/index.js` |
| F11 | continuable 子代理的 durable descriptor（version 2）已记录 resolved `agentProvider`/`agentModel`/`persona`/`toolFilter`，供 cold-resume 重建——我们的路由结果天然可持久化、可展示 | `dsh-subagent/lib/types/descriptor.d.ts` |
| F12 | agent-preset 是**插件组合（composition）**而非"角色"概念；角色语义最接近的是 `dsh-persona` 的 `deployment:persona` 段落 | `dsh-agent-presets/README.md`、`dsh-persona/README.md` |
| F13 | 设置持久化：`ctx.settings`（`SettingsProvider`）+ `settingsNamespace()`/`installSettingsSection()` + Schemastery `z` schema + `settings.mutate`（路径 op + `expectedRevision`）；`dsh-settings-file` 落盘 settings.yaml | `dsh-settings/lib/types/index.d.ts`、`dsh-llm-pi-ai/lib/index.js:1846-1865` |
| F14 | 凭据三原则：配置只存引用（`apiKeyEnv`）不存值；消费者每次 `resolve`；空值即缺失。Web 侧 `credentials.describe/set/unset` | `dsh-credentials/lib/types/index.d.ts`、`dsh-credentials-local/README` |
| F15 | UI 挂载：slot 系统 `ctx.slots.inject()`，设置页 = 注册 `settings.section`（`{id, order, label}`）；模型目录数据来自 host API `llm.providers`/`llm.models`；表单需手写（`dsh-client-schema-form` 仅提供 helper，无通用渲染器） | `dsh-client-ui-slots/lib/types/index.d.ts`、`dsh-client-ui-settings/lib/types/client/contract/slots.d.ts`、`dsh-client-schema-form/README` |
| F16 | 插件模块形态：`export const name / inject: string[] / Config: z<Config> / function apply(ctx, config)`；npm 生态命名惯例为 `dsh-plugin-*`（如 `dsh-plugin-product-subagents`、`dsh-plugin-deepeye`） | `dsh-tool-ralph/lib/types/index.d.ts`、npm 检索 |
| F17 | 委派子代理的审批策略被固定为 `never`，沙箱范围继承父会话——子代理无权弹审批，模型配置插件不得引入新的授权面 | `dsh-subagent/README.md`（Delegated policy） |
| F18 | `agent/request` 为 waterfall，可在子 agent 创建窗口内改写 `provider/model`（`LlmCallConfig`）——这是"装饰层"方案的技术底座 | `dsh-agent/lib/types/runtime-types.d.ts`、`dsh-agent-loop/lib/index.js:674` |

### 2.2 现状结论

1. **能力缺口**：模型/供应商只能"按工具实例静态固定"（F2）或"继承父 agent"（F3）；对话中与设置界面均无 per-subagent 选择入口（F1/F7）；无"职责/角色"概念（F12）。
2. **地基完好**：seam 层的 per-start 覆盖、persona、descriptor 持久化均已实现且被 `workflow` 工具验证（F3/F4/F10/F11）。**本插件不需要改内核，只需要"补一层产品化"**。

---

## 3. 行业调研

### 3.1 同类产品现状

| 产品 | 现状 | 参考 |
|---|---|---|
| **Claude Code** | subagent 定义文件（markdown frontmatter）支持 `model:` 字段；但长期存在"被忽略、继承父模型"的 bug 与大量需求（模型别名、相对档位 + 上下限策略） | [#44385](https://github.com/anthropics/claude-code/issues/44385)、[#32725](https://github.com/anthropics/claude-code/issues/32725)、[#34821](https://github.com/anthropics/claude-code/issues/34821)、[#81050](https://github.com/anthropics/claude-code/issues/81050) |
| **OpenAI Codex** | 社区持续要求 per-subagent model/provider/profile 选择与 reasoning_effort 配置；Multi-Agent v2 引入 per-thread 运行时路由 | [#14039](https://github.com/openai/codex/issues/14039)、[#11795](https://github.com/openai/codex/issues/11795) |
| **Cline** | subagents 支持按 agent 配置模型 | [Cline Subagents](https://mintlify.wiki/cline/cline/features/subagents) |
| **DSH 自身** | `workflow` 工具已支持 per-subagent `provider`/`model` 覆盖（F4）——官方路线已承认"子代理可用不同模型"是正当需求 | 见 §2 F4 |

### 3.2 行业共识与设计启示

1. **"编排者用强模型、执行者用便宜/专用模型"是主流成本策略**——per-subagent 模型选择是刚需，不是小众需求；
2. 成熟的形态是**声明式角色文件 + 可选运行时覆盖**（Claude Code 的 frontmatter、Cline 的 agent 配置）；
3. 需要**护栏**：默认值、允许列表（allowlist）、无效配置的回退，避免模型自由选择导致成本失控（对应 Claude Code #81050 的 "relative bias + cap/floor" 思路）；
4. 已有的 npm 包 `dsh-plugin-product-subagents`（见 [npm](https://www.npmjs.com/package/dsh-plugin-product-subagents)）说明 DSH 生态已有 subagent 产品化插件的先例，**立项前需查证其功能避免重名/重复**（本次调研因网络策略未能读取其 README，列为开放问题 OQ-6）。

### 3.3 DSH 官方资料

- 仓库：[deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness)（"Everything is a Plugin"）
- 官方 subagent 子系统文档：[docs/subsystems/subagent.zh.md](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/subagent.zh.md)
- 官方插件开发教程：[docs/cordis-tutorial/01-first-plugin.md](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/cordis-tutorial/01-first-plugin.md?plain=1)
- Cordis 入门：[docs/cordis-primer.zh.md](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/cordis-primer.zh.md)

---

## 4. 术语表

| 术语 | 含义（本插件语境） |
|---|---|
| **LLM 供应商 / provider route** | 实际发起大模型请求的供应商路由：`deepseek-official`、pi-ai 的某个 profile route 等。即 `AgentOptions.provider` 的值（F9） |
| **subagent provider（传输）** | 子代理的运行载体：`spawn`（无父上下文）、`fork`（继承父历史）、`acp` 等。即 `ctx.subagents.start(name, …)` 的 `name`。**与上一条不是一回事**（F9） |
| **模型名 / model** | 供应商目录中的模型 id，如 `deepseek-chat`、某路由的 `gpt-4o-mini` 等。即 `AgentOptions.model` 的值 |
| **角色模板（Role Template）** | 本插件的核心数据对象：一个命名的 subagent 角色（职责描述 + persona 文案 + 可选绑定 provider/model/reasoningEffort/工具过滤） |
| **persona** | 注入子代理的"人设/职责"系统提示段落（DSH 机制：`request.persona`，shadow `deployment:persona`） |
| **委派（delegation）** | 主 agent 通过 subagent 类工具发起子代理任务的一次调用 |
| **settings 命名空间** | DSH 设置持久化的分区标识（小写 kebab-case），如 `agent-default-model`、`llm-pi-ai`；本插件用 `subagent-director` |
| **回退链（fallback chain）** | 模型/供应商的解析优先级：对话内参数 > 角色绑定 > 插件默认 > 父 agent 继承 |
| **capability** | subagent provider 声明支持的启动期特性（`persona`/`toolFilter`/`depthLimit`/`outputSchema`），服务在启动前校验并拒绝不支持请求 |

---

## 5. 用户画像与用户故事

| 画像 | 特征 | 核心诉求 |
|---|---|---|
| **P1 成本敏感的个人开发者** | 单一 DeepSeek key，希望杂活（格式化、翻译、文案）用便宜模型 | 按角色分模型省钱 |
| **P2 多供应商/多网关用户** | 主 agent 用 A 供应商，子代理经 pi-ai 网关用 B 供应商 | 供应商级路由 + 凭据管理 |
| **P3 团队/项目负责人** | 希望团队统一"代码审查员、调研员、测试员"等角色与分工规范 | 角色模板 + 职责规划 + 团队共享配置 |

**用户故事（US）**

- **US1**：作为 P1，我想在设置里建一个"翻译员"角色并绑定便宜模型，之后在对话中直接说"让翻译员翻译这段文档"，子代理就用我指定的便宜模型执行，不用每次重复指定。
- **US2**：作为 P2，我想为"调研员"角色选择"供应商 X + 模型 Y"，并在该供应商不可用时得到清晰报错而不是悄悄退回主模型。
- **US3**：作为 P2，我想在单次委派时临时指定一个与角色默认不同的模型（例如"这次用最强模型审查关键代码"）。
- **US4**：作为 P3，我想把角色配置导出/纳入版本库，让团队成员共享同一套分工规范。
- **US5**：作为所有用户，我在不配置任何角色时，subagent 行为应与 DSH 现状完全一致（继承父模型），插件零侵入。

---

## 6. 功能需求（FR）

> 约定：`FR-x.y` 为子项；每条附验收标准（AC）。优先级见 [§10](#10-需求优先级与里程碑)。

### FR-1 供应商（LLM provider route）选择

**描述**：用户可为"subagent 默认模型"与每个"角色模板"选择 LLM 供应商。

**需求细节**

- **FR-1.1** 供应商候选项来源于 `ctx.llm.listProviders()` 注册的可配置 provider 目录（含 `displayName`、`active` 状态、`settingsNs`），并在设置 UI 中以可读名称展示（F8/F15）。
- **FR-1.2** 用户可浏览并选择任一"可路由"供应商；不可用（adapter 未注册/未配置/未激活）的供应商应灰显并附原因（F8 的 `routable` 概念）。
- **FR-1.3** 选择供应商后，模型下拉仅列出该供应商目录中的模型（联动过滤）。
- **FR-1.4** 未选择供应商时，该角色不覆盖供应商（回退到插件默认/父 agent 继承）。

**验收标准**

- **AC-1.1** 设置 UI 的供应商下拉至少展示 `deepseek-official` 与所有已配置的 pi-ai route，显示名称与 host API `llm.providers` 返回一致。
- **AC-1.2** 标记为不可用的供应商不可选中，且 UI 展示不可用原因。
- **AC-1.3** 切换供应商后，模型下拉立即重载且不含其他供应商的模型 id。

### FR-2 模型名（model）选择

**描述**：用户可为 subagent 默认值与每个角色选择具体模型名。

**需求细节**

- **FR-2.1** 模型候选项来源于 `ctx.llm.listModels(provider)`（host API `llm.models`，含 `groups + failures` 结构），支持搜索/过滤。
- **FR-2.2** 允许手填模型 id（目录之外的自定义模型：gateway 私有模型）——与 `agentDefaultModel` 的设计一致（官方 README："The service does not validate catalog membership"）。
- **FR-2.3** 已保存但目录中不存在的模型：设置 UI 明示"目录中不存在"，运行时按 FR-8 的规则处理（报错或回退，可配置）。
- **FR-2.4** 可选设置 `reasoningEffort`（若供应商支持）。

**验收标准**

- **AC-2.1** 模型下拉与搜索框可用，手填的模型 id 可保存。
- **AC-2.2** 目录加载失败时 UI 显示失败信息（复用 `failures` 通道），不阻塞手填路径。

### FR-3 配置层级与回退规则（解析链）

**描述**：一次委派的最终 provider/model 按以下优先级解析：

```text
1. 工具调用时模型显式填写的参数（per-call，FR-5）
2. 调用时指定的"角色模板"所绑定的 provider/model（FR-4）
3. 插件配置的默认角色（defaultRole）或插件默认 provider/model
4. 父 agent 的 provider/model（DSH 现状，F3 resolveChildAgentOptions）
```

**需求细节**

- **FR-3.1** 解析结果在委派发起前计算完毕，注入 `SubagentStartRequest.agentOptions`（仅覆盖用户显式配置过的字段，未配置字段不注入，保持"字段级覆盖"语义，F3）。
- **FR-3.2** 回退到第 4 层时，不注入任何 `agentOptions` 字段，行为与无插件时逐字节一致（保证 KV cache 与既有行为稳定，US5）。
- **FR-3.3** 解析过程对每次委派可重放、可审计（日志记录：请求参数、命中层级、最终值）。

**验收标准**

- **AC-3.1** 构造"四层各有值/各缺字段"的用例矩阵，逐层断言最终 `agentOptions` 与设计一致（含"未配置字段不出现"）。
- **AC-3.2** 未做任何配置时，插件对 subagent 调用路径零改动（diff 级别验证）。

### FR-4 角色模板（职责/角色规划）

**描述**：定义命名的 subagent 角色，承载"主 agent 与 subagent 分工"的语义。

**数据结构（草案）**

```ts
interface RoleTemplate {
  displayName: string            // 角色名，如 "代码审查员"
  description: string            // 职责描述：给主 agent 看（何时委派给这个角色）
  persona?: string               // 注入子代理的 persona 文案（它的行为准则）
  provider?: string              // 绑定的 LLM 供应商（可选）
  model?: string                 // 绑定的模型（可选）
  reasoningEffort?: string       // 可选
  toolFilter?: { allow?: string[]; deny?: string[] }  // 可选：工具收窄
}
```

**需求细节**

- **FR-4.1** 支持增删改角色模板（至少 1 个，无上限或合理上限如 50）。
- **FR-4.2** `description` 用于向**主 agent**注入委派指引（FR-6）；`persona` 通过 `request.persona` 注入**子代理**（FR-5/F10）。
- **FR-4.3** 每个角色可独立绑定或不绑定 provider/model；绑定行为遵守 FR-3 解析链。
- **FR-4.4** 角色模板持久化在 settings 命名空间（FR-10），可随 settings.yaml 共享/版本化（US4）。
- **FR-4.5** 内置 2-3 个示例角色模板（如"调研员——绑定便宜模型"、"代码审查员——绑定强模型"），开箱即用。
- **FR-4.6**（P2）角色模板支持 `toolFilter` 收窄子代理工具面（需 provider `toolFilter` capability，F3）。

**验收标准**

- **AC-4.1** 新建角色 → 保存 → 重启 DSH 后角色仍存在且字段完整。
- **AC-4.2** 指定角色发起委派后，子代理会话中能观察到 persona 文案生效（子代理行为/输出体现角色约束）。
- **AC-4.3** 删除角色后，引用它的默认配置被自动清理或阻止删除并提示。

### FR-5 委派路径集成（自定义 subagent 工具）

**描述**：提供模型可用的委派工具，在调用时支持选择角色/模型/供应商。

**需求细节**

- **FR-5.1** 注册一个模型可见工具（默认 `toolName: 'subagent_role'`，可配置，不得与已加载的 `subagent`/`subagent_fork`/`workflow`/`ralph` 重名），schema 在 `description`/`prompt` 基础上增加可选参数：

```jsonc
{
  "role":     { "type": "string", "description": "角色模板名（可选），未填用默认角色" },
  "provider": { "type": "string", "description": "LLM 供应商覆盖（可选），与 role 冲突时以显式 provider/model 为准" },
  "model":    { "type": "string", "description": "模型名覆盖（可选）" }
  // 复用 dsh-tool-subagent 的背景模式参数语义（run_in_background）
}
```

- **FR-5.2** 工具实现仿 `dsh-workflow-worker-thread` 的 `startChild`（F4）：把解析出的 provider/model 组装进 `agentOptions`，把角色 persona 组装进 `request.persona`，调用 `ctx.subagents.start(config.provider, request)`；provider 绑定沿用 `dsh-tool-subagent` 的配置语义（默认 `spawn`，可配置 `fork`）。
- **FR-5.3** 支持前景与后台两种模式（对齐 `dsh-tool-subagent` 的 `enableRunInBackground`/`backgroundMode` 语义；P0 至少支持前景 + one-shot 后台；P1 支持 continuable，其 descriptor 已记录模型与 persona，cold-resume 无丢失，F11）。
- **FR-5.4** 未填任何可选参数时，请求组装结果与 `dsh-tool-subagent` 的固定 config 路径等价（FR-3 第 3/4 层）。
- **FR-5.5**（P2）可选开关 `replaceStockSubagent`：卸载内置 `subagent` 工具实例并让本工具顶替 `subagent` 名字，以获得无感升级体验（须处理 toolName 冲突与 `tool:<toolName>` 系统提示段落迁移）。

**验收标准**

- **AC-5.1** 模型可看到并正确使用 `role`/`provider`/`model` 三个可选参数（schema 与描述被注入主 agent 上下文）。
- **AC-5.2** 指定 `provider=X, model=Y` 的委派，子代理实际发出的模型请求 `provider/model` 为 X/Y（可通过会话日志/descriptor 验证）。
- **AC-5.3** 指定不存在的 provider/model 时，工具返回结构化错误（FR-8），不静默回退。
- **AC-5.4** 后台委派与前景委派结果行为一致（任务收集、取消与 `dsh-tool-subagent` 对齐）。

### FR-6 主 agent 委派指引（职责规划对主 agent 的呈现）

**描述**：让主 agent"知道"有哪些角色、每个角色适合干什么、默认用什么模型。

**需求细节**

- **FR-6.1** 通过 `ctx.systemPrompt.section()` 注册一个系统提示段落（仿 `dsh-tool-subagent` 的 `tool:<toolName>` 段落，F15 同源机制），内容由角色模板实时生成：
  - 每个角色的 `displayName` + `description` +（若绑定）模型信息；
  - 委派建议：任务匹配 → 角色 → 工具参数（`role` 名）。
- **FR-6.2** 角色模板变更后，该段落对**新会话/新组装**生效（与 DSH 其他系统提示段落一致）；正在进行的会话沿用旧段落（KV cache 稳定，NFR-3）。
- **FR-6.3** 无任何角色时不注册该段落（零侵入，US5）。

**验收标准**

- **AC-6.1** 有角色时，主 agent 会话请求的 system prompt 中出现角色清单段落；无角色时不出现。
- **AC-6.2** 新增/删除角色后，新会话的段落内容相应变化。

### FR-7 subagent 结果可观测性（显示实际使用的模型）

**描述**：用户能看到每个子代理实际用了哪个供应商/模型。

**需求细节**

- **FR-7.1**（P1）在 subagent 会话详情/目录树中展示该子代理的 provider/model（数据源：continuable descriptor 的 `agentProvider`/`agentModel`，F11；one-shot 子代理可从其 session/agent options 读取）。
- **FR-7.2**（P2）在委派结果/消息中附带"模型：X/Y"标记。

**验收标准**

- **AC-7.1** 对至少一条 continuable 子代理记录，UI 展示的 provider/model 与 descriptor 记录一致。

### FR-8 错误处理与回退策略

**描述**：配置失效、供应商不可用等异常的可预期行为。

**需求细节**

- **FR-8.1** 工具参数显式指定的 provider/model 无效 → **拒绝执行**，返回结构化错误（指明哪个值无效、可用值提示），绝不静默换模型（US2）。
- **FR-8.2** 角色模板绑定的模型失效（目录未列或 adapter 不服务该 route）→ 默认**报错并说明**；用户可在设置中选择"回退到父 agent 模型"策略（每角色或全局开关）。
- **FR-8.3** 目录/发现服务暂不可用 → 不阻塞委派（仅影响 UI 提示），运行期以 adapter 实际服务能力为准。
- **FR-8.4** 所有错误信息不得包含凭据值（FR-11 安全）。

**验收标准**

- **AC-8.1** 无效 provider 的委派返回包含"无效值 + 可用列表"的错误文本。
- **AC-8.2** 开启"回退"策略且角色模型失效时，委派以父模型完成，并产生可见告警。

### FR-9 设置 UI（Settings 界面）

**描述**：在 DSH 设置界面提供可视化管理。

**需求细节**

- **FR-9.1** 注册 `settings.section`（如 `id: 'subagent-director'`，`order` 置于 Models 相关页面之后），含两个区域：①subagent 默认模型（provider/model/reasoningEffort）；②角色模板列表（卡片式：名称、描述、persona、模型绑定）。
- **FR-9.2** 供应商/模型下拉数据来自 host API `llm.providers`/`llm.models`（F15）；复用 `ModelSelection`、`ModelProviderGroup` 等既有类型；不复用 `ModelDirectory`（F7 的 agent-busy 限制）。
- **FR-9.3** 表单手写渲染，辅以 `dsh-client-schema-form` 的 `rehydrateSchema`/`nodeAtPath`/`hasPath`/`setPath`/`deletePath`/`validateDraft`（F15）。
- **FR-9.4** 写入走 `settings.mutate`（路径 op + `expectedRevision`），并发冲突时提示重试。
- **FR-9.5** 字段级"恢复默认"（`deletePath` 语义：用户覆盖字段消失即回退到 schema 默认）。
- **FR-9.6** 国际化：注册 `LocaleNamespaceMap`，提供中/英文案（NFR-7）。
- **FR-9.7**（P2）在会话输入框（composer）提供"本次委派模型"快捷选择，或与现有 `conversation.input.model` seat 并行——需绕过 F7 限制，仅影响新委派而非当前会话。

**验收标准**

- **AC-9.1** 设置页出现在设置 UI，可与 Models 页并存，重启后配置保留。
- **AC-9.2** 所有 CRUD 操作即时反映到 settings.yaml（可人工核对）。
- **AC-9.3** 两个浏览器同时编辑时，后写者收到 revision 冲突提示而非静默覆盖。

### FR-10 持久化与配置 schema

**描述**：配置以 DSH settings 标准机制持久化。

**settings 命名空间**：终稿为 `subagent-director`（初稿曾用 `subagent-options`，已按文首命名决议作废）。草案 schema：

```ts
const Config = z.object({
  defaultProvider: z.string().optional(),
  defaultModel: z.string().optional(),
  defaultReasoningEffort: z.string().optional(),
  defaultRole: z.string().optional(),            // 默认角色模板 id
  fallbackOnInvalid: z.boolean().default(true),  // 角色模型失效时回退父模型
  roles: z.dict(z.object({
    displayName: z.string(),
    description: z.string(),
    persona: z.string().optional(),
    provider: z.string().optional(),
    model: z.string().optional(),
    reasoningEffort: z.string().optional(),
    toolFilter: z.object({ allow: z.array(z.string()).optional(), deny: z.array(z.string()).optional() }).optional()
  }))
})
```

**需求细节**

- **FR-10.1** host 侧用 `installSettingsSection()` 注册命名空间与 schema，`validate` 做跨字段校验（如"角色绑定的 provider 在 `ctx.llm` 目录中存在，或允许手填"），写时拒绝非法值（F13）。
- **FR-10.2** 凭据不在本命名空间存值：如角色绑定第三方供应商，key 管理沿用各供应商既有 settings 命名空间（如 `llm-pi-ai`）+ `credentials`（F14），本插件只引用供应商 route 名。
- **FR-10.3** 配置变更对正在运行的会话不产生回溯影响（新会话/新委派生效；continuable 子代理以其 descriptor 记录为准，F11）。

**验收标准**

- **AC-10.1** 非法配置（空 displayName、未知枚举等）在 UI/API 写入口被拒绝并提示。
- **AC-10.2** settings.yaml 中本命名空间无任何明文密钥。

### FR-11 安全与策略合规

- **FR-11.1** 不改变子代理审批策略（保持 DSH 既定的 `never`，F17）；不引入新的提权面。
- **FR-11.2** 供应商选择仅允许枚举已注册 route；工具参数中的 provider 值同样校验（防 SSRF/任意路由注入）。
- **FR-11.3** 日志/错误/UI 中凭据字段 redact（复用 settings `role('secret')` 语义，F13/F14）。
- **FR-11.4** 不拦截、不缓存模型流内容（仅做路由，不做内容改写）。

### FR-12 文档与帮助

- **FR-12.1** 插件 README：安装（`dsh plugin add`）、配置示例（cordis.patch.yml + 设置界面）、角色模板示例、与 `subagent`/`workflow`/`ralph` 的关系说明。
- **FR-12.2** 设置页内帮助文案：解释"供应商 vs 传输 provider"的区别（F9）、回退链（FR-3）。

---

## 7. 非功能需求（NFR）

| # | 需求 | 说明 |
|---|---|---|
| NFR-1 | **兼容性** | 目标 DSH `0.1.0-rc.3`（`@deepseek-ai/*` 同版本）；对 `dsh-tool-subagent`/`dsh-subagent` 官方包零修改（树外插件）；新 rc 发布后需回归（风险 R-1） |
| NFR-2 | **性能** | 路由解析为纯内存计算（<1ms 量级），不阻塞委派；设置 UI 目录加载走既有 host API，不新增轮询 |
| NFR-3 | **KV cache 稳定性** | 工具 schema、系统提示段落、`agentOptions` 注入均"前缀稳定"：配置变化只影响新会话/新组装；对既定会话不产生历史重算（F2/F6 文档中的 KV Cache 约定） |
| NFR-4 | **可靠性** | 解析与校验失败必须可诊断：结构化错误 + 日志（FR-8）；插件自身异常不得吞掉委派结果 |
| NFR-5 | **可测试性** | 解析链（FR-3）、schema 校验（FR-10）、工具组装（FR-5）为纯函数/可单测模块，交付 ≥ 关键路径 80% 覆盖 |
| NFR-6 | **热更新/HMR** | client 插件支持 HMR（开发期 `pnpm run dev:web` watcher）；host 插件改动按 DSH 插件加载机制（profile 重启）生效 |
| NFR-7 | **i18n** | 中英双语（`LocaleNamespaceMap`），中文为首要语言 |
| NFR-8 | **可移植性** | 不依赖 Windows 特有特性；凭据/路径一律走 DSH 抽象 |

---

## 8. 架构与方案设计

### 8.1 总体架构（数据流）

```text
┌────────────────────────────── 用户/模型 ──────────────────────────────┐
│  设置界面（settings.section 'subagent-director'）        对话中委派       │
│   · 默认模型 · 角色模板 CRUD                     subagent_role 工具      │
│        │  settings.mutate / describe               args: {role,           │
│        ▼                                           provider, model, ...} │
└───────────────────────────────────────────────────────────────────────┘
                    │                              │
                    ▼                              ▼
┌─────────────────────────── Host 插件（树外包） ─────────────────────────┐
│                                                                         │
│  1. 配置层：installSettingsSection('subagent-director', Config, ...)    │
│        └─ validate: provider/model 存在性、角色引用完整性               │
│  2. 路由解析器 RouteResolver（纯函数）：                                 │
│        resolve(args, config, parentOptions) → { agentOptions?, persona? }│
│        解析链：参数 > 角色 > 插件默认 > 父 agent（FR-3）                 │
│  3. 委派工具：定义工具 'subagent_role'（仿 dsh-workflow startChild）    │
│        └─ ctx.subagents.start(provider, {                               │
│             agentOptions, persona, prompt, parent, signal, ... })       │
│  4. 主 agent 指引：ctx.systemPrompt.section(...) 生成角色清单段落       │
│                                                                         │
│  依赖注入（inject）：['llm','subagents','settings','systemPrompt','tools']│
└────────────────────────────────────────────────────────────────────────┘
                    │  ctx.subagents.start（seam，官方，不改）
                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  dsh-subagent：resolveChildAgentOptions(parent, requested, depth)        │
│      └─ 继承父模型，requested 字段级覆盖（F3）                           │
│  in-process driver：ctx.agents.create({ agentOptions, setup })           │
│      └─ persona 经 applyChildComposition shadow（F10）                   │
│  descriptor：continuable 记录 agentProvider/agentModel/persona（F11）    │
└─────────────────────────────────────────────────────────────────────────┘
                    │
                    ▼
           ctx.llm：adapter route 实际发起模型请求（deepseek-official / pi-ai …）
```

### 8.2 主机侧（host）插件设计

| 模块 | 职责 | 关键 API（事实依据） |
|---|---|---|
| `settings-section.ts` | 注册 `subagent-director` 命名空间 + schema + validate + setSource/onChange | `installSettingsSection`（F13，参照 `dsh-llm-pi-ai/lib/index.js:1846-1865`） |
| `route-resolver.ts` | FR-3 解析链纯函数 | 输入 `args/config/parent.options`，输出 `{agentOptions?, persona?, role}` |
| `delegation-tool.ts` | 定义 `subagent_role` 工具 + execute | 仿 `dsh-tool-subagent`（F1/F2）+ `dsh-workflow-worker-thread/lib/index.js:479-492`（F4） |
| `guidance.ts` | 主 agent 角色清单段落 | `ctx.systemPrompt.section()`（F15 同源） |
| `index.ts` | 插件入口：`name/inject/Config/apply` | F16 |

**关键实现要点**

1. **provider 双重命名空间**（F9）：工具/配置里的 `provider` 参数指 **LLM route**；`ctx.subagents.start()` 的传输 provider 名来自插件配置 `subagentProvider`（默认 `spawn`），两者绝不混用；
2. **字段级覆盖**（F3）：`agentOptions` 只注入用户显式配置过的字段（`provider`/`model`/`reasoningEffort`），未配置字段不注入，交由 seam 继承父值；
3. **persona capability 预检**：绑定 persona 的角色在委派前检查 `ctx.subagents.getProvider(name).capabilities.persona`，不支持则报错提示换传输 provider 或去 persona（F3/F10）；
4. **continuable 支持**（P1）：角色模型/persona 通过 `startContinuable` 的 spec 传入，descriptor 自动记录，cold-resume 保持（F11）。

### 8.3 客户端（client）插件设计

| 模块 | 职责 | 关键 API |
|---|---|---|
| `apply(ctx)` | 注册 `settings.section`（id `subagent-director`）+ locale 命名空间 | `ctx.slots.inject()`（F15） |
| `store.ts` | `SnapshotStore`：拉取 `llm.providers` + `llm.models` + settings describe；写 `settings.mutate` + `expectedRevision` | 参照 `dsh-client-ui-settings-models/lib/types/client/store.d.ts`（F15） |
| `SubagentOptionsSection.tsx` | 两个区域：默认模型行 + 角色模板卡片列表 | 手写表单 + `dsh-client-schema-form` helpers（F15） |
| `RoleCard.tsx` | 单角色编辑：名称/描述/persona/供应商下拉/模型下拉/推理强度/删除 | 同上 |

**数据契约**（host API，均为既有）：`llm.providers` → `ConfigurableProviderView[]`；`llm.models` → `{ groups, failures }`；`settings.describe/mutate`；类型复用 `ModelSelection`、`ModelProviderGroup`（F15）。**不复用** `ModelDirectory`（对 subagent 会话 `agent-busy`，F7）。

### 8.4 实现方案对比（决策）

| 方案 | 描述 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| **A. 自定义委派工具**（推荐 P0） | 新工具 `subagent_role`，schema 加 `role/provider/model`，execute 组装 `agentOptions`/`persona` | 完整支持 per-call 覆盖与角色选择；`workflow` 已验证链路；不动官方包 | 新增工具名，模型需学习；与内置 `subagent` 并存时需指引 | **主方案** |
| B. provider 装饰器（P2 可选） | 包装 `spawn/fork` provider，在转发前按配置改 `request.agentOptions` | 对 `ralph`/`workflow`/`subagent_fork` 等所有既有工具透明生效 | 无 per-call 参数通道；包装层 HMR/生命周期复杂 | 作为"全局默认模型兜底"增强项 |
| C. `agent/request` 装饰器（P2 可选） | 经 `registerContinuableSetup`/creation window 挂 waterfall 改 provider/model | 对任何子代理都生效 | 需侵入子代理 creation window；绕过 seam 的显式语义；复杂且易错 | 仅 B 不可行时考虑 |

**推荐路径**：P0 落地方案 A；P2 评估 B 作为"未走本插件工具的委派也应用默认模型"的增强（对应 ralph/workflow 兜底，F4/F5 的缺口）。

### 8.5 插件命名与打包

- npm 包名：**`dsh-plugin-subagent-director`**（命名决议见文首；对齐 `dsh-plugin-*` 惯例，F16；上线前查证与 `dsh-plugin-product-subagents` 的命名/功能冲突，OQ-6）；
- 插件名：**`subagent-director`**（`export const name`）；
- 模块形态：`name` / `inject: string[]` / `Config: z<Config>` / `apply(ctx, config)`（F16）；
- 注入服务：`['llm', 'subagents', 'settings', 'systemPrompt', 'tools']`（均为可选依赖获取，缺 `settings`/`systemPrompt` 时降级运行，参照 `dsh-agent-default-model` 的 optional get 模式）；
- 包结构：单包内含 host 插件与 client 插件两个入口（DSH 支持按目标环境选择入口，同生态惯例）。

---

## 9. UI/UX 流程设计

### 9.1 设置页布局（文字线框）

```text
设置 → Subagent 选项（settings.section 'subagent-director'）
┌────────────────────────────────────────────────────────────┐
│ Subagent 默认模型                                           │
│   供应商: [deepseek-official ▾]  模型: [deepseek-chat ▾]     │
│   推理强度: [— ▾]               （未配置 = 继承主模型）        │
├────────────────────────────────────────────────────────────┤
│ 角色模板                                         [＋ 新建角色] │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 代码审查员                            [删除] [设为默认]   │ │
│ │ 描述: 审查代码质量与安全…                                │ │
│ │ persona: 你是严谨的代码审查员…                          │ │
│ │ 供应商: [deepseek-official ▾] 模型: [reasoner ▾]        │ │
│ └────────────────────────────────────────────────────────┘ │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 翻译员（默认）                                  [删除]    │ │
│ │ 供应商: [pi-ai: my-gateway ▾] 模型: [qwen2.5-7b ▾]      │ │
│ └────────────────────────────────────────────────────────┘ │
│ ⓘ 供应商 = LLM 服务商/网关；传输方式(spawn/fork)在插件配置中 │
└────────────────────────────────────────────────────────────┘
```

### 9.2 对话中委派流程

1. 用户："让翻译员把 README 翻译成英文"；
2. 主 agent 依据系统提示段落（FR-6）获知存在"翻译员"角色 → 调用 `subagent_role({ role: 'translator', prompt: ... })`；
3. 插件解析链命中"角色绑定"（`pi-ai: my-gateway` + `qwen2.5-7b`），注入 `agentOptions` 与 persona；
4. seam 按既有机制创建子代理并执行；结果回传主 agent。

### 9.3 异常流程

- 角色绑定的供应商不可用 → 工具返回结构化错误（FR-8.1），主 agent 转述用户，设置页该供应商灰显（FR-1.2）；
- 用户临时指定模型 → per-call 覆盖生效，角色绑定不生效（FR-3 优先级）；
- 未配置任何内容 → 完全等同内置 `subagent` 行为（US5）。

---

## 10. 需求优先级与里程碑

| 优先级 | 需求 | 里程碑 |
|---|---|---|
| **P0（核心闭环）** | FR-1、FR-2（默认模型部分）、FR-3、FR-4.1~4.5、FR-5.1~5.4、FR-8、FR-10、FR-11、FR-12.1 | **M1：Host 插件**（配置 schema + 解析链 + 委派工具 + 校验；先以 cordis.patch.yml 配置使用） |
| **P0** | FR-9.1~9.6、FR-12.2 | **M2：设置 UI**（设置页 + 角色卡片 + 供应商/模型联动下拉） |
| **P1** | FR-6、FR-7.1、FR-5.3(continuable)、FR-4.6、FR-9.7 | **M3：体验完善**（主 agent 指引、可观测性、continuable、toolFilter、composer 快捷入口） |
| **P2** | FR-5.5、FR-7.2、§8.4 方案 B | **M4：增强**（顶替内置工具、provider 装饰器兜底 ralph/workflow、结果模型标记） |

---

## 11. 验收标准汇总

| 需求 | AC | 验证方式 |
|---|---|---|
| FR-1 | AC-1.1~1.3 | UI 手工验证 + 与 `llm.providers` 返回比对 |
| FR-2 | AC-2.1~2.2 | UI 手工 + 手填模型保存/恢复 |
| FR-3 | AC-3.1~3.2 | 单测矩阵 + 无配置时行为 diff |
| FR-4 | AC-4.1~4.3 | 设置持久化验证 + persona 注入效果人工评审 |
| FR-5 | AC-5.1~5.4 | 端到端委派 + 子代理日志/descriptor 断言 |
| FR-6 | AC-6.1~6.2 | 系统提示段落内容检查 |
| FR-7 | AC-7.1 | UI 与 descriptor 一致性 |
| FR-8 | AC-8.1~8.2 | 错误注入测试（无效 provider/模型） |
| FR-9 | AC-9.1~9.3 | UI 手工 + 并发写冲突测试 |
| FR-10 | AC-10.1~10.2 | schema 校验单测 + settings.yaml 检查 |
| FR-11 | — | 安全评审清单逐项确认 |
| NFR | — | 性能基准、KV 前缀稳定性回归、i18n 检查 |

---

## 12. 风险与开放问题

| # | 风险/问题 | 影响 | 缓解措施 |
|---|---|---|---|
| R-1 | DSH 处于 `0.1.0-rc.3`，上游 API（seam/descriptor/settings/slot 契约）可能变动 | 插件失效 | 本文档记录所依赖接口与文件路径（§2）；发布时锁定 peer 版本；跟进 rc 升级回归（NFR-1） |
| R-2 | **provider 双命名空间混淆**（subagent 传输 provider vs LLM route，F9） | 用户配置错、工具参数误解 | UI/帮助文案明确区分（FR-12.2）；参数命名用 `llm_provider` 或界面分组隔离（实现时定夺，见 OQ-1） |
| R-3 | 模型目录加载失败/route 不可路由 | UI 空目录、委派失败 | 手填通道（FR-2.2）+ 错误策略（FR-8） |
| R-4 | 工具 schema/系统提示变化引发 KV cache 失效 | 成本上升 | 遵循"新会话生效"约定（NFR-3、FR-6.2） |
| R-5 | per-call 让模型自由选昂贵模型 | 成本失控 | allowlist/默认值护栏 + 显式指定才生效（FR-3、FR-8.1） |
| R-6 | 与内置 `subagent` 工具并存造成的模型困惑（两个工具都能委派） | 行为不一致 | 默认角色体系 + 主 agent 指引（FR-6）；P2 提供顶替选项（FR-5.5） |
| R-7 | persona 依赖传输 provider 的 `persona` capability | 部分 provider（如未来 acp/远程）不支持 | 启动前 capability 预检并给出明确报错（§8.2-3） |
| **OQ-1** | 工具参数的命名：`provider`/`model` 直接沿用 `workflow` 的命名（一致性）还是加前缀区分命名空间？ | — | 评审定夺；倾向沿用 `workflow`（生态一致性） |
| OQ-2 | 角色模板与 agent-preset（composition）的关系：本期独立成 settings 概念；未来是否允许角色挂载 preset？ | — | 待 P3 场景验证 |
| OQ-3 | 是否需要 per-session 的 subagent 模型覆盖（类似主模型选择）？ | — | P2 再评估（FR-9.7 已预留） |
| OQ-4 | 是否需要成本预算（每角色 token/费用上限）？ | — | Out of scope（§1.3），观察 Claude Code #81050 走向 |
| OQ-5 | `subagent_role` 默认工具名及与 `subagent`/`subagent_fork` 的默认选择策略 | — | M1 联调时定夺 |
| OQ-6 | npm 上已有 `dsh-plugin-product-subagents`，功能与命名是否冲突（本次未能读取其 README） | 重名/重复建设 | 立项前查证；必要时更名或差异化 |

---

## 13. 参考资料

### 13.1 联网资料

- [deepseek-ai/deepseek-harness 仓库（Everything is a Plugin）](https://github.com/deepseek-ai/deepseek-harness)
- [官方文档：subagent 子系统（中文）](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/subagent.zh.md)
- [官方文档：subagent 子系统（英文）](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/subagent.md)
- [官方教程：第一个插件（cordis-tutorial）](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/cordis-tutorial/01-first-plugin.md?plain=1)
- [官方文档：Cordis 入门（中文）](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/cordis-primer.zh.md)